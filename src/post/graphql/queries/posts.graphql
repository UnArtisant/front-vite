query Posts($limit: Int!, $offset: Int){
    posts(limit: $limit, offset: $offset)
    {
        _id
        id
        title
        textSnippet
        points
        createdAt
        updatedAt
        user {_id, username,id}
    }
}